declare x, y :: integer;
        b, c, d :: boolean execute
    read x ;
    read y ;
    c := true;
    d := false;
    b := c /\ d;
    b :=  d \/ c;
    for x from 5 to 6 do
        y := 9
    done;
    b := ~c;
    while x /= y do
        x := x - y
    done;
    b := y /\ d;
    x := -y;
    write [5];
    for 5 to 7 do
        x := 10
    done
done
