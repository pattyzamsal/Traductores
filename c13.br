declare x, y :: integer;
        b, c, d :: boolean execute
    read x ;
    for x from 5 to 6 do
        y := 9
    done;
    b := ~c;
    while x /= y do
        x := x - y
    done;
    x := -y;
    for 5 to 7 do
        x := 10
    done
done
